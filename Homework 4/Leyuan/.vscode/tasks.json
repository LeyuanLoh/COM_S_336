{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "C/C++: gcc.exe build active file",
            "type": "shell",
            "command": "gcc -Wall -DUSEGLEW -o ${fileBasenameNoExtension} '${file}' -lfreeglut -lglew32 -lglu32 -lopengl32 -lm",
            "group": "build",
        }
    ]
}